# Security completion audit

Validate `COMPLETION.md` against the repository without changing files. The verdict must use a fresh test command and inspect whether secret comparison is timing-safe for equal-length tokens and rejects length mismatch.
